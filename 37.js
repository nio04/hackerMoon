const input = "hridita don't give a shit about me!";

const asciiChar = (input) => {
  const res = [];

  for (let i of arr) {
    res.push(input.charCodeAt(i))
  }

  return res;
}

console.log(asciiChar(input))
